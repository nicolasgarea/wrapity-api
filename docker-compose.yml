services:
  db:
    image: mysql:8.0
    container_name: wrapity-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: wrapity
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
